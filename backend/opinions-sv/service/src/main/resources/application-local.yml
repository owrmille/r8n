server.port: ${SERVICES_OPINIONS_PORT}
spring:
  application.name: opinions-sv
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.sql
    contexts: local
  jpa:
    hibernate:
      ddl-auto: update


  datasource:
    driver-class-name: org.postgresql.Driver
