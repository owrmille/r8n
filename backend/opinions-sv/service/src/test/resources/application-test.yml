spring:
  flyway:
    clean-disabled: false
  lifecycle:
    timeout-per-shutdown-phase: 1s
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:5432/opinions
    username: test
    password: test
    hikari:
      minimumIdle: 0
      idleTimeout: 10000
      maxLifetime: 30000